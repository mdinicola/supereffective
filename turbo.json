{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["VERCEL_ENV", "VERCEL_URL", "NEXT_PUBLIC_GOOGLE_ANALYTICS_ID"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
