/**
 * Internal operational settings (timeouts, limits, etc.)
 */
const settingsConfig = {
  delays: {
    searchDebounce: 300,
  },
  intervals: {
    dataChangeSync: 2500,
  },
  maxLengths: {
    dexTitle: 32,
    dexBoxTitle: 15,
  },
}

export default settingsConfig
